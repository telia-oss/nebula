name: Publish

on: 
  push:
    branches: [ master ]

jobs:
  publish:
    runs-on: [self-hosted, linux, ARM64]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Create links file
        run: |
          touch js/links.js
          echo 'var HRB = `https://google.com`;' >> js/links.js

